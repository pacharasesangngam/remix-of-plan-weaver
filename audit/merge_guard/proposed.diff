--- a/backend/floorplan_api.py
+++ b/backend/floorplan_api.py
@@ -1925,7 +1925,7 @@
     from collections import defaultdict as _dd
     h_buckets: dict = _dd(list)
     for seg in h_walls:
-        key = round(seg["y"] / snap)
+        key = (round(seg["y"] / snap), seg["y"])
         h_buckets[key].append(seg)
 
     merged_h: list = []
@@ -1935,7 +1935,7 @@
     # Group V-walls by quantized x-axis
     v_buckets: dict = _dd(list)
     for seg in v_walls:
-        key = round(seg["x"] / snap)
+        key = (round(seg["x"] / snap), seg["x"])
         v_buckets[key].append(seg)
 
     merged_v: list = []
